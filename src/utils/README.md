# Utils

Pure, reusable utility functions live here when they are not tied to React, Next.js, or a specific feature.

Prefer `src/lib` for framework-adjacent helpers and `src/features/*` for feature-owned helpers.
