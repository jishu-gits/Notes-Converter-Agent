# Notes Feature

Reserved for future generated notes views, note cards, and note editing experiences.

Shared formatting helpers should live in `src/utils` or `src/lib` only when they are not feature-specific.
